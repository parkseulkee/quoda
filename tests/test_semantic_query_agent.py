"""Tests for SemanticQueryAgent — legacy file kept for historical reference.

These tests covered the old generate_sql() / SemanticMatcher-based API
which was replaced in Task 7. See tests/test_semantic_agent.py for new tests.
"""
# Old tests removed: SemanticQueryAgent no longer accepts `matcher` argument
# and no longer exposes a `generate_sql()` method.
# The new API is tested in tests/test_semantic_agent.py.
